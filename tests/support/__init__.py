"""Test support for spec-artifacts-iso.

Nothing here is module code. The FR-007 reference mapping lives in
``reference_mapping`` and is a test oracle: the module ships data (schemas,
mappings, golden records) and no Python that builds a record.
"""
